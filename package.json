{
  "name": "smart-pos-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "deploy": "npm run build && echo 'Deploy completed'",
    "clean": "npm run clean --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}